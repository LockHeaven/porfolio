---
import GithubIcon from "./icons/GithubIcon.astro";
import LinkIcon from "./icons/LinkIcon.astro";
interface Props {
  title: string;
  img: string;
  imgTitle: string;
  githubUrl: string;
  liveUrl: string;
}

const { title, img, imgTitle, githubUrl, liveUrl } = Astro.props;
---

<section class="flex gap-3 w-full h-full p-8">
  <picture class="aspect-[9/16] md:aspect-square min-w-fit h-auto object-cover">
    <img
      src={img}
      alt={imgTitle}
      class="rounded-lg h-full w-full object-cover object-center"
    />
  </picture>
  <div class="flex flex-col w-full justify-between gap-3">
    <h2 class="text-white text-xl font-semibold">{title}</h2>
    <slot name="techs" />
    <slot />
    <div class="flex gap-4 justify-end text-white">
      <a
        class="cursor-pointer rounded-full hover:scale-125 hover:-translate-y-1 transition"
        ><LinkIcon size="h-8 w-8" /></a
      >
      <a
        class="cursor-pointer rounded-full hover:scale-125 hover:-translate-y-1 transition"
        ><GithubIcon size="h-8 w-8" /></a
      >
    </div>
  </div>
</section>
